model_type: lamp_bc
model_path: ""
precision: "32"
action_dim: 23
num_action_chunks: 1
temporal_ensemble_decay: 0.25
# The self-contained artifact owns prior type and latent dimensions.  Set a
# hand_prior mapping explicitly only when an additional launch-time assertion
# is desired.
hand_prior: null
